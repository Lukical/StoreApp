# StoreApp

# Sobre

O StoreApp é um app para a realização de compras do usuario em uma loja virtual.

## Rodando Aplicação

Executando o app:
```bash
  # Buildar Container
  docker-compose build
  # Iniciar Container
  docker-compose up -d
```
